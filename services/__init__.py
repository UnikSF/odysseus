# services/__init__.py
"""Service layer — plug-in capabilities for the chat core.

Each submodule (search, memory, youtube, hwfit, stt, tts) is imported
directly where used; there is no top-level facade to keep in sync.
"""
